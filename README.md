# LNG-Market-Topology

**Inferring opaque counterparty behavior in global LNG markets through physical and institutional constraints.**

Players can obfuscate their trade execution — split orders across brokers, vary timing, randomize cargo sizes — but they cannot evade the physical and institutional constraints of the network. A cargo still takes 25 days from Sabine Pass to Tokyo Bay. A regasification terminal still has a finite number of slots. JERA's fiscal year still ends on March 31. This framework treats those invariants as the basis for inference.

---

## What This Project Does

LNG-Market-Topology models the global LNG market as a constrained directed graph — terminals as nodes, shipping routes as edges, institutional rules as boundary conditions — and uses deviations from expected flow patterns to infer what major market participants are likely doing, and why.

The core logic: when physical or institutional constraints become binding, the set of feasible actions for any given player shrinks dramatically. A buyer who must fulfill a Take-or-Pay obligation before fiscal year-end, through a congested canal, into a nearly-full terminal, has very few degrees of freedom. The market signals generated by that player — curve deformation, spread compression, vessel rerouting — become increasingly deterministic. This framework detects those signals and computes a probability distribution over possible counterparty behaviors.

---

## Relationship to the LNG Project Ecosystem

This project is the inference layer that sits on top of three existing projects. Each project was built independently but produces outputs that serve as direct inputs here. The integration is architectural — these are separate repositories, not shared codebases — connected through a common data interface.

### [Global-LNG-Arbitrage-Monitor](https://github.com/EdisonLee9111/-Global-LNG-Arbitrage-Monitor)

The Arbitrage Monitor computes netback economics for LNG cargoes across JKM, TTF, and Henry Hub, incorporating shipping route selection, boil-off losses, and freight costs. In this project, its role is to define the **rational baseline**: given current prices and physical costs, what flow pattern would a profit-maximizing player choose? Any significant deviation from this baseline — a cargo routed away from the highest-netback destination, a spread left unexploited — becomes a candidate signal for the inference engine.

Specific outputs consumed:
- Per-route netback spreads (used as the null hypothesis for flow direction)
- Shipping cost estimates by route (Panama vs. Suez/Cape), used to calculate whether observed rerouting is economically rational
- Monte Carlo spread distributions, providing confidence intervals around the "rational" flow pattern

### [Structural-Event-Study-Framework](https://github.com/EdisonLee9111/Structural-Event-Study-Framework-for-LNG-Energy-Markets)

The Event Study Framework detects structural breaks in LNG market relationships — joint shifts in curve shape, volatility term structure, convenience yield, and option-implied density. Here, it serves as the **anomaly trigger**: when the framework identifies a statistically significant break (via Hotelling T² on the joint signal vector) that cannot be attributed to a public exogenous shock, the inference engine activates and asks: what counterparty behavior could have generated this pattern?

Specific outputs consumed:
- Structural break timestamps and magnitude (triggers for the inference loop)
- Transmission channel decomposition (which market dimensions shifted, informing which behavioral hypotheses are plausible)
- Convenience yield residuals (physical scarcity premium changes that flag non-public physical market activity)

### [LNG-Alpha-Feed](https://github.com/EdisonLee9111/LNG-Alpha-Feed-trading-signal-system-design)

Alpha-Feed classifies real-time news flow from the Bluesky firehose by structural market impact category rather than surface sentiment. In this project, it provides **causal gating**: when the inference engine detects an anomaly, Alpha-Feed's output helps determine whether there is a public information explanation (a reported outage, a policy announcement, a weather event) or whether the anomaly is likely driven by private counterparty action.

Specific outputs consumed:
- Event classification tags (supply disruption, policy shift, demand signal, etc.)
- Temporal alignment between detected anomalies and public news flow (if a structural break precedes any public reporting, the private-action hypothesis strengthens)

### How They Connect

```
┌─────────────────────────────────────────────────────────┐
│                 LNG-Market-Topology                     │
│              (Inference & Attribution)                   │
│                                                         │
│   ┌──────────┐  ┌──────────────┐  ┌──────────────┐     │
│   │ Topology │  │   Bayesian   │  │   Shadow     │     │
│   │  Graph   │──│  Inference   │──│   Price      │     │
│   │          │  │ (Two-Pass)   │  │   Engine     │     │
│   └──────────┘  └──────┬───────┘  └──────────────┘     │
│                        │                                │
│         ┌──────────────┼──────────────┐                 │
│         │              │              │                 │
└─────────┼──────────────┼──────────────┼─────────────────┘
          │              │              │
   ┌──────┴──────┐ ┌─────┴─────┐ ┌─────┴──────┐
   │  Arbitrage  │ │   Event   │ │   Alpha    │
   │   Monitor   │ │   Study   │ │   Feed     │
   │             │ │           │ │            │
   │  Rational   │ │  Anomaly  │ │  Causal    │
   │  Baseline   │ │  Trigger  │ │  Gate      │
   └─────────────┘ └───────────┘ └────────────┘
```

---

## Architecture

The system is organized in three layers, each with a distinct epistemological function.

### Constraint Layer

Maps what is physically and institutionally possible. This layer is largely deterministic.

**Physical constraints** — the directed graph of LNG infrastructure:
- Terminal nodes: liquefaction plants (with nameplate capacity and historical utilization), regasification terminals (with slot availability and seasonal maintenance windows)
- Route edges: shipping lanes with transit time distributions, canal passage constraints (Panama draft limits, Suez scheduling), and Cape of Good Hope alternatives
- Vessel fleet: ship positions, speed profiles, laden/ballast status (via AIS data where available)

**Institutional constraints** — the calendar of hard deadlines and contractual boundaries:
- Fiscal year cycles (JERA/KOGAS: March 31; Chinese SOEs: December 31 with mid-year review)
- Take-or-Pay contract windows (annual delivery quantity obligations with shortfall penalties)
- Seasonal maintenance schedules for major terminals
- Regulatory triggers (Chinese NDRC winter supply mandates, typically issued October-November)

**Constraint Transmission Channels**:
Constraints propagate through the network via two distinct mechanisms:
- **Direct Physical Transmission (Topological Binding)**: Players directly relying on a severed route or bottleneck face an absolute collapse of their feasible action space. Their resulting behavior is deterministic and highly price-insensitive.
- **Indirect Market-Mediated Transmission (Induced Binding)**: Players whose physical supply chains are intact are still affected. The price-insensitive actions of topologically constrained players endogenously shift global market clearing prices (e.g., JKM surges), subjecting physically unaffected players to a tightened, higher-cost operating environment.

Implementation: `topology_graph.py` builds the physical graph, `vulnerability_matrix.py` pre-computes exposure metrics; `state_calendar.py` maintains the institutional prior calendar.

### Signal Layer

Ingests observable market data and computes deviations from the constraint-implied baseline. This layer is stochastic.

**Price-based signals** (from existing projects):
- Netback spread residuals: actual spread minus Arbitrage Monitor's computed rational spread
- Curve shape metrics: front-month vs. deferred slope, term structure inflection points
- Convenience yield residuals: deviations from the Event Study Framework's estimated physical premium
- Option-implied density shape: skewness and kurtosis shifts in Breeden-Litzenberger densities

**Physical flow signals** (new to this project):
- Vessel rerouting events: cargoes diverted from expected destination based on laden departure direction
- Abnormal steaming speed: vessels accelerating beyond economic optimum (suggesting urgency) or slow-steaming (suggesting floating storage or destination optionality)
- Terminal utilization spikes: sendout rates approaching nameplate capacity at specific regasification facilities

**Cross-market signals** (planned for Phase 4):
- FX hedging flow anomalies in USD/JPY correlating with LNG procurement timing
- Freight market activity (spot TC rates, period charter fixtures) inconsistent with public cargo flow

### Inference Layer

Computes a posterior probability distribution over counterparty behaviors, conditioned on the observed signal combination and the active constraint set.

**Two-Pass Inference Structure**:
Because binding constraints generate highly deterministic behavior, the inference engine operates sequentially rather than computing a joint posterior over all players simultaneously:
- **First Pass (Topological/Direct Binding)**: The model targets players facing the tightest physical and institutional constraints (producing lowest posterior entropy). The output is a highly localized, deterministic demand shock vector (e.g., KOGAS must procure *X* spot cargoes, irrespective of price, before *Date Y*).
- **Transition (Market Clearing)**: First-pass outputs are aggregated against global spare supply to endogenously calculate the shift in market conditions (e.g., JKM price spike).
- **Second Pass (Market-Mediated/Induced)**: The model targets physically unaffected players (e.g., JERA) using the shifted market conditions as an updated prior. Their hypothesis space is evaluated against the new equilibrium, determining whether they will absorb the elevated marginal cost, hold their position, or actively resell into the tightened market to capture the premium generated by First-Pass players.

**Bayesian attribution model** (`bayesian_attr_model.py`):

The model maintains a set of behavioral hypotheses for each major player type — seasonal restocking, opportunistic arbitrage, portfolio rebalancing, distressed procurement, strategic pre-positioning. For each hypothesis *h*, given observed signal vector *s* and active institutional state *c*:

```
P(h | s, c) ∝ P(s | h, c) · P(h | c)
```

- The prior `P(h | c)` is time-varying, driven by the institutional calendar. Probability of JERA restocking behavior spikes in Q4 of their fiscal year (January-March); probability of Chinese SOE distressed procurement spikes when NDRC winter mandates are active.
- The likelihood `P(s | h, c)` maps each behavioral hypothesis to its expected signal signature under the current constraint regime. When Panama Canal is congested, a restocking hypothesis for an Asian buyer implies Cape routing, which implies specific transit time delays and freight cost signatures.

**Shadow price engine** (`shadow_price_engine.py`):

When observed behavior violates the Arbitrage Monitor's rational baseline, the shadow price engine quantifies the implied cost of the hidden constraint driving that behavior. The engine operates in dual-mode depending on the inference pass:
- **Route Deviation Mode (First Pass & Normal conditions)**: If a cargo is routed to a lower-netback destination, the shadow price is the netback differential — the minimum value the player must be assigning to the non-economic factor (contract, mandate) to rationalize the decision.
- **Price Premium Mode (Second Pass)**: In a market-mediated constraint scenario, the shadow price evaluates the marginal spot premium a player is willing to absorb (or capture via resale) over its long-term contract baseline, without requiring a physical route deviation.

This provides a scalar measure of "how much is this player paying to do something that looks irrational?" — which in turn helps disambiguate between institutional pressure (high shadow price, suggesting hard deadline or political mandate) and strategic positioning (low shadow price, suggesting the deviation is near-marginal and likely opportunistic).

---

## Data Sources

| Data Type | Source | Access | Status |
|---|---|---|---|
| LNG spot prices (JKM, TTF, HH) | ICE, CME | Subscription / delayed free | Synthetic fallback implemented in Arbitrage Monitor |
| Vessel positions (AIS) | MarineTraffic, Kpler | API (paid) / limited free tier | Planned |
| Terminal capacity & utilization | GIIGNL, IEA, GIE (ALSI/AGSI+) | Public annual reports + some API | Planned |
| Panama Canal transit data | ACP (Panama Canal Authority) | Public monthly reports | Planned |
| Institutional calendars | Public filings, regulatory announcements | Manual curation | Planned |
| LNG cargo fixtures | Spark Commodities, Refinitiv | Subscription | Planned |

For initial development, the framework uses synthetic data generators calibrated to realistic market parameters, consistent with the approach in the Arbitrage Monitor. Each module documents where synthetic data is used and specifies the interface for real data integration.

---

## Roadmap

**Phase 1 — Physical Topology & Institutional Calendar**
Build the directed graph representation of the global LNG network (terminals, routes, bottlenecks) and the institutional constraint calendar. Pre-compute the network vulnerability matrix to identify specific player exposures. Validate graph structure against historical flow data. Deliverable: a queryable constraint model that, given a player and a date, returns the feasible action set and network exposure.

**Phase 2 — Signal Integration & Shadow Price**
Connect Arbitrage Monitor output to generate real-time deviation metrics. Implement the shadow price dual-mode calculation for observed deviations. Case study structured in two layers:

- **Layer 1 (Historical Validation):** 2023-2024 Panama Canal drought — reconstruct Asian buyer behavior under canal capacity constraints and validate the framework against reported cargo rerouting.
- **Layer 2 (Real-Time Stress Test):** Apply the **Two-Pass Inference Structure** to the 2026 Hormuz Strait crisis as a live out-of-sample test, generating falsifiable predictions that distinguish between topological binding responses and market-induced strategic behaviors under acute chokepoint disruption.

Full narrative, methodology, and hypotheses: [case_study_narrative.md](case_study_narrative.md).

**Phase 3 — Bayesian Inference Engine**
Implement the full posterior computation over counterparty behaviors. Integrate Event Study structural break detection as the trigger mechanism and Alpha-Feed news classification as the causal filter. Validate against known historical episodes where major player behavior was retrospectively reported (e.g., PetroChina's 2021 winter restocking, European buyers' post-Freeport LNG scramble in 2022).

**Phase 4 — Cross-Market Signals & Entropy Detection**
Extend signal layer to include FX hedging flow and freight market activity. Implement entropy-based detection for algorithmically obfuscated order flow — identifying periods where market noise is "too smooth" relative to expected variance under natural supply-demand dynamics.

---

## Falsifiable Hypotheses

These hypotheses are formally developed in the [case study narrative](case_study_narrative.md) (Sections 5–6), where each is operationalized with specific data requirements and decision thresholds. The framework makes specific, testable predictions:

**Constraint Binding → Inference Precision**: When a physical bottleneck becomes binding (e.g., Panama Canal transit slots drop below a threshold), the posterior entropy over counterparty behavior should decrease — meaning inference becomes sharper. If bottleneck proximity has no measurable effect on inference quality, the core premise of constraint-based inference is wrong.

**Institutional Calendar → Regime Switching**: Prediction accuracy for major players should show statistically significant improvement when institutional priors are active versus inactive. Specifically: JERA-related behavioral predictions should be more accurate in January-March (fiscal year-end) than in other quarters; Chinese SOE predictions should be more accurate in October-December (post-NDRC winter mandate). If calendar-conditioned priors do not outperform flat priors, the institutional constraint layer adds no value.

**Shadow Price → Behavioral Disambiguation**: When the shadow price engine identifies a high implied cost of deviation (above a calibrated threshold), the underlying cause should be retrospectively attributable to an institutional hard constraint (contract obligation, regulatory mandate) rather than strategic positioning, at a rate significantly above chance. If high shadow prices are equally likely to reflect strategic and institutional causes, the disambiguation logic fails.

**Entropy Anomaly → Large Player Activity**: Periods identified by the entropy detector as having abnormally low variance (relative to the null distribution of natural market noise) should correlate with subsequent reporting of large-scale counterparty activity (tender awards, major fixture announcements). If detected low-entropy periods show no association with ex-post confirmed large trades, the entropy signal is noise.

---

## License

MIT
